{
  "name": "toskania",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/validate-trip.js && node scripts/convert-webp.js && vite build",
    "preview": "vite preview",
    "test": "node --test scripts/validate-trip.test.js scripts/maps-tiles.test.js scripts/reservations.test.js"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "vite": "^6.3.5"
  }
}
